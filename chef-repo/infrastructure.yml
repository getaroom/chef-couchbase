cookbooks:
- logrotate:
- couchbase:
- chef_handler:
- minitest-handler:

environments:
- production:

roles:
- server:
- server_with_custom_memory_quota:

data bags:
- apps:
  - pillowfight

nodes:
- ec2 1:
  - role[server]
  - --no-host-key-verify -x ubuntu -G default -I ami-3c994355 -f c1.medium -d ubuntu12.04-gems --region us-east-1 -E production
- ec2 1:
  - role[server] role[server_with_custom_memory_quota]
  - --no-host-key-verify -x ubuntu -G default -I ami-3c994355 -f c1.medium -d ubuntu12.04-gems --region us-east-1 -E production
